# Recording Rules

This directory contains the stable metrics API consumed by dashboards.

## Domains

- compute.rules.yaml
- storage.rules.yaml
- network.rules.yaml
- operations.rules.yaml

Dashboards should reference recording rules rather than complex raw PromQL wherever practical.
